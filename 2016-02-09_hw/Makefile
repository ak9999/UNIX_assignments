# Abdullah Khan

# Compile and compiler flags:
CC = gcc
CFLAGS = -Wall -std=c11 -pedantic
DEBUG = -g

all:
	make editor

editor:
	$(CC) editor.c -o editor $(CFLAGS)

clean:
	(rm -f *.o;)
	(rm -f *.out;)
	(rm -f editor;)
